<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.altamirano.fabricio.lamanzana">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.SearchCompanyActivity"></activity>
        <activity
                android:name=".ui.CustomerActivity"
                android:label="@string/title_activity_customer" />
        <activity
                android:name=".ui.CompanyMainActivity"
                android:label="@string/title_activity_company_main"
                android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.StoreDetailsActivity" />
        <activity android:name=".ui.AllCountriesActivity" />
        <activity android:name=".NewChangeActivity" />
        <activity android:name=".ui.ChooseModeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>